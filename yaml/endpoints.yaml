define: &base_url 'https://practice.expandtesting.com/notes/api'

endpoints:
  health: 'health-check'
  notes: 'notes'
  login: 'users/login'
  register: 'users/register'
  profile: 'users/profile'
  delete: 'users/delete-account'